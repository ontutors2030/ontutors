@model IEnumerable<OPTFS.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<div class="pageHeader">
    <h1 class="boldItalicText">Courses Page</h1>
</div>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
@*<table class="table ListTable">
        <thead class="datagridheader">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Subject)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FromDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ToDate)
                </th>
                <th>
                    Days
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LogoUrl)
                </th>
                <th>
                    Rating
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Active)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="tr">
                    <td>
                        <a asp-controller="CourseDetails" asp-action="Index" asp-route-id="@item.Id">
                            @Html.DisplayFor(modelItem => item.Name)
                        </a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Subject.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FromDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ToDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Sat) Sat
                        @Html.DisplayFor(modelItem => item.Sun) Sun
                        @Html.DisplayFor(modelItem => item.Mon) Mon
                        @Html.DisplayFor(modelItem => item.Tue) Tue
                        @Html.DisplayFor(modelItem => item.Wen) Wen
                        @Html.DisplayFor(modelItem => item.Thi) Thi
                        @Html.DisplayFor(modelItem => item.Fri) Fri
                    </td>
                    <td>
                        <img src="~/@item.LogoUrl" style="height:40px;width:40px;" class="img-thumbnail" />
                    </td>
                    <td>
                        @await Html.PartialAsync("RatingViewerPartial", item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Active)
                    </td>
                    <td>
                       <a asp-action="Details" asp-route-id="@item.Id"
                           class="btn-sm btn-primary" style="margin:3px">
                            <i class="fa fa-info-circle" aria-hidden="true"></i>
                        </a>

                        <a asp-action="Edit" asp-route-id="@item.Id"
                           class="btn-sm btn-info" style="margin:3px">
                            <i class="fa fa-pencil-square" aria-hidden="true"></i>
                        </a>

                        <a asp-action="Delete" asp-route-id="@item.Id"
                           class="btn-sm btn-danger" style="margin:3px">
                            <i class="fa fa-trash-o" aria-hidden="true"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>*@

<div class="row">
    @if (Model != null && Model.Count() > 0)
    {
        foreach (var course in Model)
        {
            <div class="col-md-6">
                @await Html.PartialAsync("CoursePartial", course)
                <br />
            </div>
        }
    }
</div>